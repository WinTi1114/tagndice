# 이 저장소는 화이트리스트 방식입니다: 기본적으로 모든 파일을 무시하고,
# 핵심 파일만 아래에서 명시적으로 추적 대상에 포함시킵니다.
# 새 파일을 저장소에 추가하고 싶으면 이 파일 맨 아래에 !경로 를 추가하세요.

*

# 디렉터리는 계속 탐색 (하위 파일의 !규칙이 적용되도록)
!*/

# --- 핵심 파일 화이트리스트 ---
!.gitignore
!README.md
!spec.md
!design_note_26_base.md
!멀티플레이_방_시스템_설계.md
!webapp/index.html
!webapp/test_app4.py
!render.py
!build_xlsx.py
