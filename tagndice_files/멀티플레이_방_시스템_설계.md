# 멀티플레이 방(초대 링크) 시스템 — 설계 제안 (v1, 논의용 초안)

이 문서는 실제 구현 전에 방향을 맞추기 위한 **설계 제안**입니다. 아직 코드에는 반영되지 않았습니다.

## 1. 배경 및 목표

지금의 `index.html`은 브라우저 `localStorage`에만 저장되는 **정적 단일 HTML 파일**입니다. 서버가 없고, 같은 브라우저·같은 기기 안에서만 캐릭터 시트가 유지됩니다.

사용자가 제안한 것: 마스터(GM)가 여러 명이서 각자 세션을 진행할 때 방이 서로 분리되어야 하므로,
- GM이 새 방을 만들고
- 초대 링크를 생성해서 플레이어들과 공유하고
- 플레이어는 링크로 들어와서 그 방 안에서 캐릭터 시트를 실시간으로 공유
- 안 쓰는 방(무활동, 또는 모두 나간 방)은 자동으로 사라짐

이걸 구현하려면 "브라우저에만 저장" 구조를 벗어나 **여러 사람의 브라우저를 실시간으로 이어주는 서버**가 필요합니다 — 이 프로젝트에서 처음으로 생기는 백엔드입니다.

## 2. 제안하는 전체 구조

- **기존 방식은 그대로 유지**합니다. 방을 안 만들면 지금처럼 로컬에만 저장되는 1인용 시트로 동작 — 아무것도 안 바뀝니다.
- URL에 방 코드가 있을 때만(`?room=ABC123` 형태) "방 모드"로 전환되어 서버와 실시간으로 연결됩니다.
- 백엔드는 **Supabase**(Postgres DB + Realtime + Edge Functions)를 제안합니다.
  - 이유: 회원가입만으로 무료로 시작 가능하고, DB·실시간 동기화·정기 정리 작업(Edge Function)이 한 서비스 안에 다 있어서 별도로 서버를 직접 만들고 호스팅할 필요가 없습니다. 이 프로젝트 규모(취미용, 소규모 그룹)에는 무료 한도로 충분합니다.

### 무료 한도로 충분한지 (2026년 8월 기준 확인)

| 항목 | 무료 한도 |
|---|---|
| 프로젝트 수 | 2개 |
| DB 용량 | 500MB |
| 월간 활성 사용자(MAU) | 50,000명 |
| Realtime 동시 접속 | 200 |
| Realtime 월간 메시지 | 200만 건 |
| 월간 아웃바운드 트래픽 | 5GB |

취미용으로 GM 한두 명, 세션당 플레이어 몇 명 규모라면 이 한도에 걸릴 일은 사실상 없습니다.

**단, 한 가지 유의할 점**: 무료 프로젝트는 **7일간 아무 활동이 없으면 자동으로 일시정지**됩니다. 데이터는 그대로 보존되고(최대 1년 내 언제든), 대시보드에서 "Resume" 버튼 한 번만 누르면 즉시 복구됩니다 — 데이터 손실은 없지만, 일주일 넘게 안 쓰다가 갑자기 세션을 열면 GM이 미리 한 번 깨워둬야 할 수 있습니다. (유료 플랜으로 올리면 이 일시정지가 없어지지만, 지금 규모에선 필요 없어 보입니다.)

## 3. 데이터 모델 (초안)

```
rooms
  id            방 코드 (예: 6자리 랜덤 코드, 초대 링크에 들어감)
  created_at
  last_active_at   (자동 정리 판단 기준)
  gm_token         GM만 아는 값 — GM 권한 확인용

characters
  id
  room_id       ─ rooms.id 참조
  slot_name     플레이어 구분용 이름/슬롯
  sheet_json    캐릭터 시트 전체 필드(체력·태그·인벤토리 등)를 통째로 저장
  updated_at

gm_secrets  (일반 characters와 분리된 별도 테이블)
  room_id
  secret_text   GM 전용 비밀 메모
```

**보안상 중요한 부분**: 지금은 GM 비밀 메모를 "PIN 잠금"으로 화면에서만 숨기고 있는데, 이건 같은 기기 안에서만 의미가 있는 방식입니다. 여러 사람이 네트워크로 접속하는 방 구조에서는 플레이어도 개발자도구로 서버에 직접 요청을 보낼 수 있기 때문에, **GM 비밀 데이터는 반드시 서버(Supabase Row Level Security)가 "GM 토큰을 아는 사람만 조회 가능"하도록 강제**해야 합니다. 클라이언트 쪽 PIN 입력창은 지금처럼 유지하되, 실제 방어선은 서버 쪽 규칙이 되어야 합니다. 이 부분은 타협 없이 반드시 지킬 원칙으로 설계에 넣겠습니다.

## 4. 방 생명주기

- **생성**: GM이 "방 만들기" 클릭 → 랜덤 코드 발급 → 초대 링크(`.../?room=ABC123`) 표시 + 복사 버튼
- **참여**: 플레이어가 링크 클릭 → 방 코드로 접속 → 자기 캐릭터 슬롯 선택 또는 새로 생성
- **자동 소멸**: 사용자가 말씀하신 "안 쓰거나 모두 나가면 사라짐"은 두 가지 신호를 합쳐서 구현하는 걸 제안합니다.
  1. **무활동 정리**: 마지막 활동 후 일정 시간(예: 24~48시간) 지나면 정기적으로 방을 정리(Supabase Edge Function을 하루 한 번 정도 실행)
  2. **전원 퇴장 감지**: 실시간 접속 현황(Presence)으로 "지금 아무도 없음"은 알 수 있지만, 탭을 그냥 닫는 경우 등 연결 끊김 감지가 몇 초~몇십 초 지연될 수 있어 100% 즉시는 아닙니다.

  → 그래서 **"나가는 즉시 삭제"보다는 "얼마간 비어있으면 정리"** 쪽이 안정적입니다. 정확히 몇 시간 후로 할지는 사용자 확인이 필요합니다 (5번 참고).

## 5. 웹앱 쪽 변경 방향

- 지금의 `persist()`/`persistQuiet()`(로컬 저장) 위에, 방 모드일 때는 Supabase로도 같이 저장하는 계층을 추가
- 다른 사람이 고친 내용은 Supabase Realtime 구독으로 받아서 화면에 반영
- 동시 수정 충돌은 각자 자기 캐릭터만 수정하는 게 대부분일 것으로 예상되어, 우선은 "마지막에 저장한 값이 반영" 정도의 단순한 방식으로 시작하는 것을 제안 (필요하면 나중에 보강)

## 6. 예상 구현 단계

1. Supabase 프로젝트 연결 + 방 생성/참여(코드 공유, 입장 확인만 — 아직 실시간 동기화 없음)
2. 캐릭터 시트 필드 실시간 동기화 (플레이어 ↔ GM)
3. GM 비밀 데이터 서버 단 보호(RLS) 적용
4. 무활동 방 자동 정리

## 7. 사용자가 준비해주셔야 하는 것

- 무료 Supabase 계정/프로젝트 생성은 제3자 서비스 가입이라 제가 대신 만들어드릴 수 없습니다 — 직접 가입 후 프로젝트를 만들고, "프로젝트 URL"과 "anon public key" 두 값만 전달해주시면 나머지 연결 작업은 제가 진행합니다. 가입 과정 자체도 원하시면 단계별로 같이 안내해드릴 수 있습니다.

## 8. 아직 확인이 필요한 점

- **자동 삭제 타이밍**: "얼마간 비어있으면 정리" 방식이 괜찮으실지, 괜찮다면 대략 몇 시간~며칠 정도가 적당할지
- **플레이어 구분**: 링크만 있으면 누구나 들어와서 빈 캐릭터 슬롯을 골라 쓰는 정도로 충분한지, 아니면 이름 등으로 최소 구분이 필요한지
- **방당 인원 규모**: 보통 몇 명 정도(플레이어 수)를 기준으로 UI를 짜면 될지
- Supabase 가입을 직접 진행하실 수 있는지, 아니면 가입 과정부터 같이 안내가 필요하신지

## 참고 자료

- [Project Pausing — Supabase 공식 문서](https://supabase.com/docs/guides/platform/free-project-pausing)
- [Supabase Pricing Page and Costs Explained (2026)](https://schematichq.com/blog/supabase-pricing)
